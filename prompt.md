# Team Paper Reader / Internal AlphaXiv Prompt

## 文档目的

这份文档是未来 AI 持续迭代本项目时的核心需求文档与执行约束。

目标：基于当前 `paper-reader`，逐步升级出一个适合小团队内部共同分享、阅读、讨论、处理论文的系统。产品方向可以参考“内部版 AlphaXiv”，但必须坚持一个原则：

**先做最小可用 MVP，不要一开始把工程做得无限复杂。**

未来 AI 在每次迭代前都应完整阅读本文件，并遵守文中的优先级、范围控制和阶段划分。

---

## 一、最高优先级原则

### 1. MVP First

所有迭代都必须优先追求最小可用版本，而不是一次性实现完整理想形态。

具体要求：

- 每次迭代只解决一个清晰问题，交付一个可验证增量
- 优先做会改变团队日常使用方式的核心能力
- 不要为了“未来也许会用到”而提前引入复杂设计
- 能用简单方案完成的，不要先上重型方案
- 在多人协作能力真正跑通之前，不要过早投入复杂推荐系统、复杂前端重写、复杂检索系统

### 2. 团队化优先

这个项目未来不再只是个人阅读器，而是一个小团队共用的论文阅读与讨论平台。

因此默认前提应是：

- 论文是共享资产，不只是某个人的本地文件
- Prompt 结果是共享资产，不应重复计算
- 推荐、评论、标签、聊天是团队上下文的一部分
- 普通成员不应接触路径、存储目录、模型底层配置等系统细节

### 3. 管理员控制关键配置

凡是会影响团队共享行为或系统稳定性的能力，都应由管理员控制。

至少包括：

- 核心 Prompt 的新增、修改、下线
- 存储路径与导入源配置
- 模型可用性和配额
- 权限与用户管理
- 系统级标签治理

### 4. 先数据库化元数据，再做复杂协作

当前项目的大量状态依赖本地文件、JSON 和文件扫描，这适合单机模式，但不适合团队协作。

因此后续演进时要坚持：

- 原始 PDF/文档文件可以继续保留在文件系统或对象存储
- 用户、论文元数据、推荐、评论、标签、Prompt、处理记录、聊天记录等，必须逐步进入数据库
- 搜索和筛选必须以数据库索引为基础，而不是继续依赖全量文件扫描

### 5. AI 结果必须可复用、可追踪

任何 AI 生成内容都必须知道：

- 是哪篇论文的结果
- 是哪个 Prompt 版本生成的
- 是谁触发的
- 用了什么模型
- 何时生成
- 是否共享
- 是否命中缓存

---

## 二、当前项目基线

当前项目已经具备这些基础能力：

- 本地论文库扫描与浏览
- PDF / DOC / DOCX 预览
- Prompt 管理与批量处理
- Prompt 结果保存为 Markdown
- 外部来源归档（如 Hugging Face Daily Papers）
- 离线阅读包导出
- 基础登录保护

但当前仍然是明显的单机阅读器架构，主要限制包括：

- 只有单个用户名密码入口，不是真正的多用户系统
- Prompt 定义和部分状态保存在本地 JSON 文件中
- 论文对象本质仍然偏“文件路径”，不是稳定的共享实体
- 没有推荐、点赞、评论、标签协作、聊天等真正的团队对象
- 搜索还没有数据库级优化能力

因此，接下来的任务不是简单加几个页面，而是把系统从“个人工具”升级为“团队产品”。

---

## 三、产品目标

### 目标一句话版本

把当前 `paper-reader` 升级成一个适合小团队内部使用的论文协作平台，让大家可以：

- 共同分享与推荐论文
- 共同阅读、讨论、点赞、评论论文
- 共同复用论文的 Prompt 处理结果
- 通过标签和搜索更快找到相关论文
- 后续围绕单篇论文使用共享 Chatbot 和私聊 Chatbot

### 成功标准

当 MVP 做完时，一个团队至少应能做到：

- 成员 A 上传或导入一篇论文
- 成员 B 打开后能看到这篇论文是谁推荐的
- 团队成员可以对这篇论文点赞和评论
- 如果某个共享 Prompt 已经处理过该论文，其他成员可以直接复用结果
- 团队成员可以通过标签、标题、推荐人等方式快速搜索论文
- 核心 Prompt 只能由管理员修改

如果这几点还做不到，就不要过早扩展到太多高级功能。

---

## 四、核心用户与权限模型

## 4.1 MVP 角色

MVP 阶段建议先只做两类角色，避免过度复杂：

- `admin`
  - 管理成员
  - 管理核心 Prompt
  - 管理系统设置
  - 管理导入配置
  - 管理系统级标签
- `member`
  - 上传或导入论文
  - 推荐论文
  - 点赞、评论、讨论
  - 使用共享 Prompt 结果
  - 编辑个人可编辑内容

## 4.2 Phase 2 以后再考虑的角色

只有在 MVP 稳定后，才考虑引入：

- `editor`
- `viewer`
- 项目级角色
- 更细粒度的权限矩阵

## 4.3 权限原则

MVP 中必须明确：

- 核心 Prompt 只能 `admin` 编辑
- 普通成员可以触发共享 Prompt 运行，但不能修改核心 Prompt 内容
- 存储路径、导入目录、模型配置对普通成员不可见
- 普通成员不需要手工维护文件系统路径

---

## 五、领域对象重构方向

为了支持团队协作，系统需要从“文件中心”逐步升级为“共享论文实体中心”。

MVP 至少要明确这些对象：

- `User`：用户
- `Role`：角色
- `Paper`：共享论文实体
- `PaperFile`：论文原始文件
- `PaperSource`：论文来源（本地上传、arXiv、OpenReview、外部导入等）
- `Recommendation`：推荐记录
- `Prompt`：共享 Prompt 定义
- `PromptVersion`：Prompt 版本
- `PromptRun`：某次处理记录
- `Comment`：论文评论/回复
- `Like`：点赞
- `Tag`：标签
- `PaperTag`：论文标签关系

注意：

**长期上，论文不能再仅仅等同于一个文件路径。**

同一篇论文可能：

- 被多人推荐
- 从多个来源导入
- 有多个文件副本
- 对应多份共享处理结果

所以未来必须有稳定的 `paper_id` / canonical paper 概念。

---

## 六、明确的功能规划

## 6.1 MVP 必须做的功能

以下功能属于 MVP 范围，应该优先完成。

### A. 多用户与团队基础能力

必须实现：

- 真实多用户登录，不再只用一个共享账号
- 最小 RBAC：`admin` / `member`
- 用户名、显示名、基础资料
- 基础会话管理

MVP 不要求：

- 企业 SSO
- LDAP / OIDC
- 复杂组织架构

这些放到后续。

### B. 论文推荐与共享入库

必须实现：

- 团队成员可以推荐一篇论文
- 系统能显示这篇论文是由谁推荐的
- 推荐时可以填写推荐理由
- 同一篇论文可以被多人推荐，但系统应尽量归并到同一个共享论文实体

推荐记录至少应包含：

- 推荐人
- 推荐时间
- 推荐理由
- 来源类型

### C. 共享 Prompt 结果复用

这是 MVP 的核心差异化能力之一，必须尽早实现。

必须实现：

- 同一篇论文使用同一个共享 Prompt 时，不重复处理
- 已有结果时，其他成员可直接查看和复用
- 系统能展示该结果由谁生成、何时生成、用的哪个 Prompt 版本
- 核心 Prompt 只能由管理员修改

缓存复用至少要基于：

- `paper_id`
- `prompt_version_id`
- `model`
- 内容版本或文件版本

### D. 点赞与评论

MVP 中评论系统先保持简单。

必须实现：

- 论文点赞
- 论文评论
- 评论回复（最多做一层嵌套即可）
- 显示评论人和时间

MVP 暂不强求：

- 评论富文本
- 复杂表情/反应系统
- 评论排序策略过多
- 页码级评论

### E. 标签系统（最小版）

必须实现：

- AI 自动生成标签
- 用户可手工添加标签
- 区分 AI 标签和人工标签
- 基于标签进行搜索和筛选

MVP 不要求：

- 很复杂的标签层级体系
- 标签知识图谱
- 大量同义词治理后台

但数据模型要为后续扩展留空间。

### F. 搜索优化（数据库优先）

必须实现：

- 按标题搜索
- 按作者搜索
- 按标签搜索
- 按推荐人搜索
- 按是否已处理搜索
- 按时间排序/筛选

技术上必须满足：

- 搜索基于数据库，而不是继续依赖全量文件扫描
- 常见查询有索引
- 列表有分页

### G. 团队版阅读器（最小改造版）

MVP 不要求彻底重做前端，但必须让阅读器体现“团队协作”特性。

论文详情页至少要展示：

- 论文标题、作者、来源
- 推荐人信息
- 标签
- 点赞数
- 评论入口
- 共享 Prompt 结果入口

优先级上：

- 先在现有界面中做最小可用改造
- 不要一开始就完全重写 UI

### H. 多渠道导入（最小版）

MVP 中导入功能建议控制范围。

必须做：

- 本地 PDF 上传
- arXiv ID 导入
- arXiv 链接导入
- 直接 PDF URL 导入

如果实现成本可控，可加：

- OpenReview PDF 链接导入

MVP 暂不要求：

- OpenReview 任意页面复杂解析
- 通用网页自动抽取一切论文信息
- 批量网页抓取平台化

原则是：先解决最常用、最稳定的导入路径。

---

## 6.2 Phase 2 功能

只有当 MVP 稳定后，才进入这一阶段。

### A. Chatbot：共享模式 + 私聊模式

这是重要方向，但建议放在 MVP 之后。

必须设计成两种模式：

1. `Shared Chat`
   - 每篇论文一个共享聊天线程
   - 团队成员共同看到历史问答
   - 适合共同讨论论文
   - 后续支持在讨论中 `@bot`

2. `Private Chat`
   - 每个用户针对同一篇论文拥有自己的私聊线程
   - 仅本人可见
   - 用于私人提问、学习和笔记整理

注意：

- Phase 2 可以先不做复杂多线程管理
- 先做最简单的一篇论文一个共享聊天、每个用户一个私聊线程即可

### B. 更丰富的导入渠道

包括：

- OpenReview 页面链接解析
- 通用 URL 识别
- 批量导入链接
- 导入状态页

### C. 团队首页 / Feed

包括：

- 最近推荐
- 最近热门讨论
- 最近处理完成的论文
- 最近新增标签/合集

### D. 通知系统

包括：

- 回复通知
- `@我` 通知
- 新评论通知
- 处理完成通知

---

## 6.3 Phase 3 功能

这些都不是第一阶段重点，应在前两阶段稳定后再做：

- 高亮与批注
- 页码级评论
- FAQ 沉淀
- 阅读列表 / 合集
- 相似论文推荐
- 向量检索 / 语义搜索
- 项目空间 / 小组空间
- 更复杂的权限体系
- 更复杂的推荐算法

---

## 七、明确的非目标（MVP 阶段不要做）

为了防止工程失控，MVP 阶段明确不做以下事情，除非已经证明实现成本很低且不会拖慢主线：

- 不做微服务化拆分
- 不做完全前后端分离重写
- 不做复杂推荐系统
- 不做复杂通知中心
- 不做复杂权限矩阵
- 不做复杂语义搜索平台
- 不做复杂项目空间/租户系统
- 不做过度抽象的插件化平台
- 不做与核心目标无关的炫技功能

如果某个提案不能显著提升团队共享阅读的核心体验，就应延后。

---

## 八、推荐的技术与架构方向

## 8.1 总体建议

建议采用：

- Web 应用：继续基于 Flask 演进，先模块化，不急着重写框架
- 元数据数据库：PostgreSQL
- 异步任务：Redis + 轻量任务队列
- 原始文件存储：先继续用本地磁盘 / NAS，再逐步抽象
- 搜索：优先 PostgreSQL FTS + 普通索引 + trigram

## 8.2 架构原则

- 文件用于保存原始论文和部分生成产物
- 数据库用于保存所有共享协作元数据
- 后台任务负责导入、Prompt 处理、自动标签、后续聊天问答
- 搜索、筛选、推荐、权限判断都应主要依赖数据库

## 8.3 不要过早引入的复杂方案

MVP 阶段不建议：

- Elasticsearch / OpenSearch
- 大规模对象存储改造
- 复杂事件总线
- 分布式微服务
- 向量数据库优先架构

---

## 九、MVP 最小数据模型建议

MVP 阶段优先定义这些表或等价模型：

- `users`
- `roles`
- `user_roles`
- `papers`
- `paper_files`
- `paper_sources`
- `paper_recommendations`
- `prompts`
- `prompt_versions`
- `prompt_runs`
- `comments`
- `paper_likes`
- `tags`
- `paper_tags`

可选但建议尽早考虑：

- `audit_logs`
- `saved_searches`
- `user_paper_states`

数据建模必须满足：

- 一篇论文可以有多个来源
- 一篇论文可以有多条推荐记录
- 一篇论文可以对应多个 Prompt 结果
- 一个 Prompt 可以有多个版本
- 标签可区分 AI 生成和人工维护

---

## 十、关键业务规则

## 10.1 论文去重与唯一性

系统必须尽量识别“同一篇论文”的重复导入。

MVP 去重优先级建议：

1. 外部 ID（arXiv ID 等）
2. PDF hash
3. 标题 + 作者 + 年份近似匹配

目标不是一开始做到 100% 完美，而是先避免最常见的重复。

## 10.2 Prompt 共享缓存规则

共享处理结果必须满足：

- 相同论文
- 相同 Prompt 版本
- 相同模型
- 相同内容版本

才可以直接复用。

如果 Prompt 内容变了，就应该形成新版本，而不是覆盖旧结果。

## 10.3 Prompt 权限规则

MVP 中至少要求：

- `admin` 可新增/修改/下线核心 Prompt
- `member` 可触发运行共享 Prompt
- 普通成员不可直接编辑核心 Prompt

## 10.4 用户体验规则

- 普通成员不应手工编辑磁盘路径
- 普通成员不应关心文件存储目录结构
- 团队主要看到的是论文对象，而不是服务器上的文件名/路径细节

---

## 十一、搜索与性能要求

MVP 虽然要小，但数据库和搜索优化必须尽早进入设计，否则后面会返工。

### 必须做到

- 论文列表分页
- 常见筛选字段加索引
- 标签查询可接受
- 推荐人和处理状态可搜索
- 不再依赖每次请求全量扫文件系统

### 暂不要求

- 向量召回
- 重排序模型
- 复杂个性化推荐

### 性能关注点

- 论文详情页不要一次拉取过多评论
- 搜索列表不要做过重联表
- Prompt 结果存在性最好可快速判断
- 列表页要避免实时聚合过多重型统计

---

## 十二、Chatbot 规划说明

Chatbot 是重要方向，但不是第一阶段最优先落地项。

正确顺序是：

1. 先让团队共享论文、推荐、评论、Prompt 共享跑通
2. 再在稳定的 `paper_id`、权限体系和共享上下文之上增加聊天能力

### 为什么不建议现在就把 Chatbot 做太重

因为 Chatbot 要依赖这些基础前提：

- 稳定的论文实体模型
- 清晰的共享/私有权限边界
- 可复用的文本上下文和 Prompt 结果
- 成本与速率控制
- 消息审计能力

在这些基础没稳之前，聊天能力很容易把系统复杂度拉爆。

### 但现在就要保留的设计前提

未来一定要支持：

- `Shared Chat`：团队共享问答
- `Private Chat`：个人私聊问答
- 评论区或讨论区中 `@bot`

因此当前设计时，至少要为未来保留：

- `paper_id`
- `user_id`
- 共享/私有可见性边界
- 对 AI 结果和上下文的可引用能力

---

## 十三、UI / UX 方向

MVP 阶段的 UI 改造原则是：

- 在现有阅读器基础上增量演进
- 先让团队信息显示出来
- 不急着做大改版

### 论文详情页 MVP 应新增的核心信息

- 谁推荐了这篇论文
- 推荐理由
- 标签
- 点赞和评论入口
- 共享 Prompt 结果列表
- 共享结果生成状态

### 管理员界面 MVP 应支持

- 用户列表
- 角色设置
- 核心 Prompt 管理
- 基础系统设置

### 不建议 MVP 就做的 UI 重构

- 复杂三栏工作台
- 大型前端状态管理重构
- 过于复杂的实时协作界面

---

## 十四、推荐的阶段划分

## Phase 0：建模与迁移设计

目标：

- 明确数据库模型
- 明确用户与权限模型
- 明确 `paper` 与 `paper_file` 的分离
- 明确从当前文件型系统迁移到数据库型系统的路线

完成标准：

- 能输出最小 schema
- 能解释如何兼容现有本地论文库
- 能说明哪些状态继续在文件中，哪些迁移到数据库

## Phase 1：MVP 主线

这是最关键阶段，必须优先落地。

范围：

- 多用户登录
- `admin` / `member` 权限
- 共享论文实体
- 推荐记录
- 点赞与评论
- 共享 Prompt 结果复用
- 管理员维护核心 Prompt
- AI 标签 + 人工标签
- 数据库搜索与基础索引
- 本地上传 + arXiv ID/链接导入 + 直接 PDF URL 导入

完成标准：

- 团队已经可以真实协作使用
- 共享 Prompt 结果已经跑通
- 搜索不再靠文件扫描
- 普通成员不需要接触路径配置

## Phase 2：互动增强

范围：

- Shared Chat
- Private Chat
- `@bot`
- OpenReview 扩展导入
- Feed
- 通知

完成标准：

- 团队能围绕单篇论文与 Bot 共同互动
- 个人也能有自己的私聊问答空间

## Phase 3：体验深化

范围：

- 高亮/批注
- FAQ 沉淀
- 阅读列表
- 相似论文
- 更强搜索

完成标准：

- 团队版阅读器更成熟，但不破坏前面已经稳定的主线能力

---

## 十五、未来 AI 的执行要求

未来 AI 每次迭代都必须遵守以下规则：

1. 先判断本次迭代属于哪个阶段
2. 如果需求超出当前阶段，优先压缩范围，而不是盲目全部实现
3. 先做数据模型和接口边界，再做大块 UI
4. 每次只完成一个相对闭环的增量
5. 每次迭代都补充测试
6. 每次迭代都更新本文件中的当前状态与下一步计划
7. 避免让系统再次依赖零散 JSON 作为长期核心元数据存储
8. 不要把普通用户暴露给系统路径和底层配置
9. 对共享 Prompt 结果、权限、搜索性能要持续优先关注
10. 若某项设计明显会拖慢 MVP，应主动降级方案

---

## 十六、每次迭代的标准输出格式

未来 AI 每次迭代后必须至少给出：

- 本次目标
- 实现范围
- 修改文件
- 数据模型变化
- 新增接口/页面
- 权限影响
- 测试结果
- 已知风险
- 下一步建议

同时应更新本文档中的：

- 当前阶段
- 已完成事项
- 下一步事项
- 风险与决策记录

---

## 十七、当前推荐的第一个正式任务

未来 AI 的第一轮正式工作，建议直接做下面这件事：

**基于当前项目，设计并落地 MVP 所需的最小数据库 schema 与迁移方案，至少覆盖：**

- users
- roles
- user_roles
- papers
- paper_files
- paper_sources
- paper_recommendations
- prompts
- prompt_versions
- prompt_runs
- comments
- paper_likes
- tags
- paper_tags

并回答这些问题：

- 现有本地文件库如何兼容
- 论文如何去重并形成 canonical paper
- Prompt 结果如何从文件型缓存升级为共享数据库 + 产物混合模型
- 搜索如何逐步从文件扫描切换到数据库查询

在这个任务完成前，不要急于做很重的 UI 重写和 Chatbot 扩展。

---

## 十八、开放问题

这些问题可以后续逐步确定，不应阻塞 MVP 主线：

- 登录是否接企业 SSO
- 是否引入 `editor` / `viewer`
- OpenReview 页面解析做到什么程度
- 标签是否做层级体系
- 是否做项目空间 / 小组空间
- Chatbot 成本如何控制
- 私聊聊天是否纳入管理员审计
- 后续是否接向量搜索

---

## 十九、当前状态

### 当前阶段

- 已完成 Phase 0 / Phase 1 的第一轮 MVP 落地，实现了最小数据库化和团队协作主线

### 当前已完成

- 引入数据库化元数据层（当前采用 SQLite 作为本地 MVP 方案，后续可升级到 PostgreSQL）
- 多用户登录与最小 RBAC：`admin` / `member`
- 管理员创建团队成员账号
- Prompt 改为数据库存储，并支持版本化
- 核心 Prompt 编辑仅管理员可操作
- 论文团队元数据已入库：推荐、点赞、评论、标签、来源、Prompt 运行记录
- 阅读器中已展示团队协作信息：推荐理由、标签、点赞、评论、共享结果摘要
- 搜索已接入数据库元数据匹配（标题/文件名之外，还可命中标签、推荐人、推荐理由、来源）
- 已支持最小远程导入：arXiv ID、arXiv 链接、OpenReview PDF 链接、直接 PDF 链接
- 共享 Prompt 结果会记录版本、模型、触发人和结果路径元数据

### 当前结论

- 这是一个团队协作产品升级项目，不是简单功能拼接
- MVP 依然必须聚焦：多用户、共享论文、推荐、评论、共享 Prompt 结果、标签搜索、最小导入能力
- 为了避免工程过早复杂化，当前数据库先采用 SQLite 落地；生产化 PostgreSQL 升级应放在下一阶段有明确收益时进行
- Chatbot 仍然建议放在 MVP 之后，前提是共享对象模型和权限体系已经稳定
- 当前阅读器已经完成从“个人阅读器”到“团队阅读器”的第一步，但离完整内部 AlphaXiv 还有明显增量空间

### 当前缺口 / 后续重点

- 需要进一步把搜索、列表和聚合更多地切换为数据库驱动，而不是继续依赖文件扫描作为主要入口
- 需要补更完整的迁移和运维方案，包括数据库备份、schema 升级和生产部署路径
- 需要补充更完整的用户管理能力，例如重置密码、停用账号、角色变更
- 需要继续强化论文 canonical 化和去重策略，不应长期把 `rel_path` 当作近似主键
- 需要为后续 Shared Chat / Private Chat 预留更稳定的上下文对象模型

### 下一步

- 巩固 Phase 1：继续完善数据库驱动查询、管理员后台、远程导入鲁棒性和更完整的权限治理
- 在当前对象模型稳定后，再进入 Phase 2：Shared Chat / Private Chat / `@bot` / Feed / 通知
